<h1>Welcome to the Sorting Hat!</h1>
<%= image_tag "http://flavorwire.files.wordpress.com/2009/06/sorting-hat.jpg" %>

<%= form_for @student do |f| %> <!-- you don't put @house here because you are assigning the student to a random house --> 
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :img_url %>
	<%= f.text_field :img_url %>

	<%= f.submit %>

	<% end %>